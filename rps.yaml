scripts:
  clean: 'flutter clean && flutter pub get'
  build: 'flutter build apk --split-per-abi'
  uninstall: 'adb uninstall com.mastertipsy.controlcenter'
  install: 'adb install build/app/outputs/flutter-apk/app-arm64-v8a-release.apk'
  deploy: 'rps clean && rps build && rps install'